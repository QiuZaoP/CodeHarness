# 代码库智能助手项目方案设计

## 1. 项目定位

本项目是一款对标 Claude Code 的代码库智能助手，基于自研 Agent Harness 智能体调度框架，为开发者提供仓库级代码理解、问题定位、代码修改和验证能力。

产品支持导入完整 Git 代码仓库或本地源码目录，建立可检索的代码知识索引；用户通过类似 Claude 的对话界面提出任务，智能体可以跨文件阅读和修改代码，并将所有变更以 Diff 形式展示给用户。

首期核心场景包括：

- 仓库级 Bug 定位与原因分析
- 缺陷修复和补丁生成
- 跨文件代码重构
- 单元测试生成与补充
- 代码变更预览、验证和回滚

## 2. 总体架构

系统分为前端交互层和后端智能体运行层，两层通过会话、事件流和代码变更协议连接。

### 2.1 前端交互层

前端复刻 Claude Code 的工作台体验，负责：

- 创建和切换对话会话
- 导入 Git 仓库或本地源码目录
- 展示仓库文件树、文件内容和搜索结果
- 展示智能体的思考状态、工具调用状态和执行日志
- 展示多文件 Diff、文件变更统计和验证结果
- 提供停止任务、继续任务、接受变更、拒绝变更和回滚操作

前端不负责任务拆解、代码分析和文件修改决策。前端只消费后端事件，并将用户操作转换为明确的命令。

### 2.2 后端 Agent Harness 运行层

后端负责：

- 管理项目、仓库、会话和任务生命周期
- 根据用户目标拆解任务并维护执行计划
- 管理上下文、工具权限、预算和停止条件
- 调度代码搜索、文件读取、文件修改、命令执行和测试工具
- 汇总工具结果，判断是否继续循环或结束任务
- 对文件变更进行沙箱隔离、Diff 生成和版本管理
- 将运行过程以结构化事件流推送给前端

后端内部再划分为四个能力模块：

1. **Harness 核心**：状态机、任务循环、计划、上下文、权限和错误恢复。
2. **代码解析与索引**：文件扫描、语言识别、AST 解析、符号关系和代码检索。
3. **模型网关**：统一封装大模型、Embedding 模型和重排模型，负责流式输出、重试和模型路由。
4. **工具执行层**：将文件、搜索、AST、命令和测试能力封装成受控工具，供 Harness 调用。

## 3. 完整业务运行流程

一次完整任务包含“项目准备、会话交互、智能体执行、变更验证、结果交付”五个阶段。

### 3.1 项目准备阶段：导入仓库并建立索引

1. 用户创建项目，选择导入 Git 仓库或本地源码目录。
2. 前端提交导入请求，后端创建项目记录并生成唯一 `project_id`。
3. 后端校验源码路径、Git 信息、文件权限和项目大小。
4. 系统复制或挂载源码到项目工作区，并生成基础快照。原始目录只读，后续修改在任务工作区中进行。
5. 文件扫描器遍历项目目录，过滤 `.git`、构建产物、依赖缓存、二进制文件和用户配置的忽略目录。
6. 解析模块识别文件语言，提取文件元数据、导入关系、类、函数、变量、调用关系、注释和测试文件信息。
7. 系统建立三类索引：
   - 文件索引：路径、语言、大小、更新时间、摘要。
   - 符号索引：类、函数、方法、接口、变量及其定义位置。
   - 关系和语义索引：引用关系、调用关系、依赖关系、代码片段向量。
8. 索引完成后，后端生成项目概览，包括技术栈、入口文件、测试目录、构建命令和高频模块。
9. 前端通过事件流展示扫描进度、解析进度、索引进度和异常文件；索引完成后开放对话入口。

索引不是任务执行的前置硬依赖。对于解析失败的文件，系统保留文本搜索和原文读取能力，并在项目状态中标记降级范围。

### 3.2 会话创建阶段：接收用户目标

1. 用户创建会话，选择项目和目标分支或当前工作区。
2. 后端创建 `session_id`，加载项目概览、规则文件、用户权限和会话历史摘要。
3. 用户输入任务，例如“定位登录接口偶发 500 的原因并修复，补充单元测试”。
4. 前端将用户消息以命令形式提交，后端创建一次 `task_run`，记录任务目标、工作区版本、模型配置和预算。
5. Harness 先进行任务前检查：确认工作区可写、获取当前 Git 状态、读取项目规则文件，并检查是否存在未提交变更。
6. 如果检测到可能被覆盖的用户变更，Harness 暂停并通过前端请求用户确认；未经确认不修改相关文件。

### 3.3 Harness 规划阶段：建立任务计划

Harness 不直接让模型对整个仓库自由操作，而是先将用户目标转化为结构化执行计划。

1. Harness 构造规划上下文：用户目标、项目概览、相关规则、Git 状态、最近会话摘要和可用工具说明。
2. 模型输出结构化计划，至少包含：目标、假设、待调查模块、拟调用工具、修改范围、验证方式和完成条件。
3. Harness 校验计划格式、工具权限、文件范围和预算，不接受越权路径、危险命令或无法验证的计划。
4. Harness 将计划拆分为可追踪的步骤，例如：
   - 定位请求入口和错误日志路径
   - 查找异常产生位置及其调用链
   - 阅读相关实现和已有测试
   - 形成根因假设
   - 修改实现
   - 增加回归测试
   - 执行定向测试和必要的全量检查
5. 前端展示计划和当前步骤。对高风险操作，例如删除文件、修改大量文件、执行外部命令，要求用户确认或由项目策略自动拦截。

### 3.4 Harness 执行循环：理解、行动、观察、决策

每一个任务步骤都进入统一的 Agent Harness 循环。循环的核心不是“模型输出一次答案”，而是“模型决策一次受控行动，再依据真实工具结果继续决策”。

#### 循环入口：加载状态

Harness 从任务状态中加载：

- 用户目标和当前计划
- 已完成步骤、待办步骤和失败记录
- 当前工作区快照及未提交 Diff
- 已读取文件、搜索结果、AST 关系和测试结果
- 历史消息摘要、最近工具结果和剩余上下文预算
- 当前权限、命令白名单、超时和最大循环次数

#### 第一步：构造上下文

上下文管理器根据当前步骤选择最小但足够的信息，优先加入项目概览、相关文件片段、符号关系、调用链、错误日志和最近操作结果。大文件按符号或行区间分块读取，重复内容去重，过长历史压缩成摘要。

#### 第二步：调用模型进行决策

模型根据当前上下文输出以下一种结果：

- 调用一个工具
- 按顺序调用多个无副作用工具
- 更新任务计划或提出澄清问题
- 结束当前步骤并进入验证
- 结束整个任务并生成结果说明

模型输出必须经过协议校验，工具名称、参数、路径和命令都由 Harness 重新检查，不能直接信任模型文本。

#### 第三步：执行工具

工具执行器在项目工作区中运行工具，典型工具包括：

- `list_files`：浏览目录和文件树
- `search_text`：按文本、正则或错误信息搜索
- `search_symbol`：按符号、定义、引用和调用关系搜索
- `read_file`：按文件、符号或行区间读取源码
- `read_ast`：读取语法树、定义和关系信息
- `apply_patch`：以补丁方式修改文件
- `write_file`：在受控范围内创建或覆盖文件
- `run_command`：执行白名单内的构建、格式化和测试命令
- `git_diff`：获取变更内容和统计
- `git_status`：获取工作区状态

所有工具都记录调用参数、耗时、输出摘要、错误信息和影响文件。命令执行必须使用超时、资源限制、路径隔离和命令策略校验。

#### 第四步：观察工具结果

工具结果分为成功、空结果、可恢复错误、需要确认和不可恢复错误五类。结果会被结构化处理：源码结果提取文件和行号，测试结果提取失败用例和堆栈，命令结果截取有效日志并保留原始记录。

#### 第五步：更新状态并判断下一步

Harness 根据工具结果更新任务状态：

- 证据不足：继续检索或读取相关代码。
- 假设被否定：记录原因，回退计划并建立新的调查方向。
- 找到根因：进入修改步骤。
- 修改失败：保留失败信息，调整补丁或请求用户介入。
- 验证失败：进入修复循环，最多按预算执行限定次数。
- 满足完成条件：进入结果整理。
- 触发预算、权限、超时或安全策略：暂停任务并说明原因。

之后循环回到“加载状态”，直到任务完成、暂停、取消或失败。

### 3.5 代码修改阶段：生成并应用变更

1. Harness 要求模型先说明修改目标、影响文件和预期验证方式。
2. 模型通过 `apply_patch` 产生最小化补丁，禁止直接修改项目工作区之外的路径。
3. 工具执行器应用补丁前检查文件版本、上下文匹配和冲突情况。
4. 每次补丁应用后立即生成增量 Diff，并更新工作区快照。
5. Harness 检查变更是否超出计划范围；超出范围时暂停并请求确认，或撤销本次补丁。
6. 前端实时展示变更文件、增加/删除行数、代码 Diff 和当前智能体步骤。

变更默认只写入任务工作区，不自动提交 Git，也不自动覆盖用户已有的未提交修改。用户可以在任务结束后统一接受、导出补丁、继续修改或回滚。

### 3.6 验证阶段：测试、静态检查和回归判断

验证按由小到大的范围执行：

1. 语法和格式检查，确认补丁未破坏文件结构。
2. 变更文件级检查，例如类型检查、Lint 或编译单文件。
3. 相关模块定向单元测试。
4. 受影响模块的集成测试。
5. 项目全量测试、构建或用户配置的质量门禁。

Harness 读取测试结果并将失败归因分为：代码问题、测试问题、环境问题和无关基线失败。代码问题会回到执行循环，测试问题允许模型修复测试，环境问题需要提示用户处理。验证循环必须设置最大次数和总预算，避免无限修复。

### 3.7 结果交付阶段：汇总、确认和结束

任务满足完成条件后，Harness 生成结构化结果：

- 任务结论和根因说明
- 修改文件及每个文件的变更摘要
- 关键 Diff
- 执行过的工具和验证命令
- 测试通过、失败或未执行情况
- 未解决风险、假设和后续建议

前端将结果按“总结、文件变更、Diff、验证结果、运行日志”展示。用户可以：

- 接受全部变更
- 按文件或按区块接受变更
- 拒绝或回滚变更
- 在当前工作区继续追问
- 导出补丁或交给 Git 流程处理

用户接受后，系统记录最终快照和审计信息；用户拒绝后，任务工作区回到用户选择的版本，原始项目不受影响。

## 4. Harness 状态机设计

### 4.1 任务状态

建议使用以下状态：

`CREATED` -> `PRECHECKING` -> `PLANNING` -> `EXECUTING` -> `VERIFYING` -> `READY_FOR_REVIEW` -> `APPLIED`

异常分支：

- 任意运行态 -> `WAITING_USER`：需要澄清、确认高风险操作或解决冲突。
- `EXECUTING` / `VERIFYING` -> `PAUSED`：用户主动暂停或达到临时预算。
- 任意运行态 -> `CANCELLED`：用户取消任务并保留审计记录。
- 任意运行态 -> `FAILED`：发生不可恢复错误。

### 4.2 每轮循环的统一协议

每轮执行记录建议包含：

```text
RunState {
  run_id
  session_id
  phase
  current_step
  plan
  context_refs
  tool_calls
  workspace_snapshot
  changed_files
  verification_results
  budget
  stop_reason
}
```

模型与 Harness 之间使用结构化消息：

```text
Decision {
  type: TOOL_CALL | PLAN_UPDATE | ASK_USER | VERIFY | COMPLETE
  tool_name?
  arguments?
  reason
  expected_observation
}
```

### 4.3 停止条件

任务只能在以下条件之一满足时结束：

- 用户目标已经完成，并且完成条件全部通过。
- 代码修改已经完成，但验证明确存在无法自动修复的问题，已向用户说明。
- 用户主动取消。
- 权限、安全策略、上下文、时间或成本预算耗尽。
- 工具或模型发生不可恢复故障。

## 5. 核心模块与接口边界

### 5.1 前端模块

负责页面和交互状态，包括项目选择、文件树、会话、消息流、工具状态、Diff 和验证结果。前端通过事件协议接收后端状态，不在浏览器执行具有项目写权限的命令。

### 5.2 Harness 核心模块

负责任务状态机、规划执行、上下文编排、工具选择、权限判断、预算、重试、暂停恢复和结果汇总。Harness 只依赖模型网关和工具注册接口，不关心具体模型供应商和解析器实现。

### 5.3 代码解析与索引模块

负责文件发现、语言适配、AST 解析、符号关系、索引构建和增量更新。对外提供按文件、符号、引用、调用链和语义相似度检索的统一接口，不负责对话决策和文件补丁应用。

### 5.4 模型网关模块

负责模型供应商适配、模型路由、鉴权、流式响应、结构化输出、重试、限流、Token 统计和故障降级。对 Harness 暴露统一的 `chat`、`stream` 和 `embed` 接口。

### 5.5 测试与质量模块

负责测试策略、验收标准、基准仓库、测试执行编排、结果归因和版本质量门禁。测试模块不拥有业务模块实现代码，只通过公开接口和可交付环境验证系统。

## 6. 五人开发小组分工方案

五个角色按“一个人一个主责模块”划分。每个角色可以协助其他角色排障，但代码所有权、接口维护权和最终交付责任只有一个归属人。

### 角色一：前端交互负责人

**唯一主责**：实现 Claude 风格代码工作台及前端状态呈现。

**负责内容**：

- 项目导入、项目切换和仓库文件树
- 会话列表、消息流和任务状态展示
- 工具调用进度、错误、暂停和用户确认交互
- 代码查看、行号、搜索定位和多文件 Diff
- 接受、拒绝、回滚、继续和取消任务等操作
- 对接后端会话 API、事件流 API 和 Diff API
- 前端可用性、响应式布局和基础浏览器兼容性

**不负责**：Harness 决策、模型 Prompt、AST 解析、后端命令执行、测试策略制定。

**交付物**：前端页面、状态管理、接口类型、事件流消费组件、Diff 组件和前端测试。

### 角色二：后端 Harness 核心负责人

**唯一主责**：实现 Agent Harness 运行循环和任务生命周期。

**负责内容**：

- 项目、会话、任务和工作区状态管理
- 任务前检查、计划生成、计划执行和状态机
- 上下文选择、历史摘要、Token 预算和循环次数控制
- 工具注册、工具参数校验、权限策略和安全停止
- 工具结果归一化、重试、暂停恢复和错误处理
- 变更快照、Diff 汇总和结果报告
- 对外提供会话 API、任务 API、事件流 API 和任务控制 API

**不负责**：具体模型供应商 SDK、AST 语法适配、前端页面、测试用例资产维护。

**交付物**：Harness 状态机、运行循环、上下文管理器、工具协议、后端 API 和审计记录。

### 角色三：代码解析与索引负责人

**唯一主责**：实现代码库理解、AST 解析和多维索引能力。

**负责内容**：

- 文件扫描、忽略规则和语言识别
- 支持首期目标语言的 AST 解析适配
- 文件、符号、定义、引用、调用和依赖关系提取
- 代码片段切分、摘要和向量索引
- 全量索引、增量索引和解析失败降级
- 提供文件、符号、调用链和语义检索接口
- 为 Harness 工具层提供解析查询能力

**不负责**：模型选型和请求发送、任务规划、补丁应用、用户界面、端到端验收结论。

**交付物**：解析器适配器、索引存储、索引构建任务、检索 API、解析质量报告和基准仓库索引。

### 角色四：模型网关负责人

**唯一主责**：实现模型调用统一层和模型服务稳定性。

**负责内容**：

- 对接自研或第三方大模型、Embedding 模型和重排模型
- 统一 Chat、流式 Chat、结构化输出和 Embedding 协议
- 模型路由、配置管理、鉴权、限流、重试和超时
- Prompt 模板、工具调用格式和模型输出校验支持
- Token 统计、成本统计、延迟监控和模型质量指标
- 模型不可用时的降级、熔断和错误分类

**不负责**：Harness 状态机逻辑、具体代码搜索实现、前端交互、测试用例编写。

**交付物**：模型适配器、网关 API、结构化输出协议、调用监控、模型评测数据和故障降级机制。

### 角色五：测试统筹与质量负责人

**唯一主责**：建立全链路质量体系并负责版本验收。

**负责内容**：

- 定义功能、性能、安全、稳定性和可用性验收标准
- 建立代表 Bug、重构和测试生成场景的基准仓库
- 设计 Harness 循环、工具调用、索引、模型网关和前端的测试矩阵
- 编写端到端回归测试和关键接口契约测试
- 组织变更验证、失败归因、缺陷跟踪和发布质量门禁
- 评估任务成功率、定位准确率、补丁通过率、测试通过率、延迟和成本
- 维护 CI 流程、测试数据、回归报告和版本发布清单

**不负责**：任何生产模块的长期实现和主代码所有权；发现缺陷后负责复现、归因和验收，不替代模块负责人修复。

**交付物**：测试计划、基准仓库、自动化测试、质量指标面板、回归报告和发布准入结论。

## 7. 角色交接与协作规则

### 7.1 关键交接点

| 交接点 | 上游负责人 | 下游负责人 | 交付协议 |
| --- | --- | --- | --- |
| 前端接入后端 | Harness 核心 | 前端 | OpenAPI/JSON Schema、事件类型、错误码和 Diff 数据结构 |
| Harness 使用模型 | 模型网关 | Harness 核心 | 结构化决策协议、流式事件、Token 用量和错误分类 |
| Harness 使用代码索引 | 代码解析 | Harness 核心 | 文件/符号/引用/调用链检索接口和结果行号 |
| 工具进入运行循环 | Harness 核心 | 测试统筹 | 工具注册表、参数约束、权限等级、模拟实现和契约测试 |
| 版本质量验收 | 各模块负责人 | 测试统筹 | 可部署版本、测试说明、已知问题和变更清单 |

### 7.2 统一工程规则

- 所有跨模块数据优先使用版本化 JSON Schema，禁止依赖未声明的内部字段。
- 每个接口先定义请求、响应、错误和超时行为，再开始并行开发。
- 每个模块负责人维护自己的单元测试和接口契约测试，测试统筹负责跨模块回归。
- 所有代码修改都必须能生成 Diff，并能通过快照回滚；禁止绕过工作区直接修改原始仓库。
- 工具调用必须有审计记录，命令工具必须有超时、白名单和工作目录限制。
- 每次合并至少包含实现说明、测试结果和已知风险；测试统筹拥有发布阻断权。

### 7.3 并行分支开发策略

五人分工采用按模块划分的 Git 分支并行开发模式。这里的“并行”是代码实现并行，不代表接口定义、联调和质量验收可以完全脱离主线独立进行。

建议分支结构如下：

| 分支 | 负责人 | 主要内容 |
| --- | --- | --- |
| `feature/frontend-workbench` | 前端负责人 | 工作台、会话、文件树、Diff 和事件流展示 |
| `feature/harness-runtime` | Harness 负责人 | 状态机、任务循环、上下文、工具协议和后端 API |
| `feature/code-indexer` | 解析负责人 | 文件扫描、AST、符号索引和检索接口 |
| `feature/model-gateway` | 模型网关负责人 | 模型适配、流式调用、结构化输出和监控 |
| `feature/quality-system` | 测试负责人 | 测试框架、基准仓库、契约测试和端到端回归 |

统一以 `main` 作为稳定分支，以 `develop` 或 `integration` 作为日常集成分支。各负责人从集成分支创建职责分支，完成后通过 Pull Request 合并回集成分支；集成验证通过后再合并到 `main`。

并行开发的推荐顺序：

1. **接口冻结**：Harness 负责人联合前端、解析、模型网关负责人定义 API、事件、工具、检索和模型调用协议；测试负责人同步把协议转为契约测试。
2. **基础能力并行开发**：五个职责分支分别实现自己的模块。前端使用 Mock 事件和 Mock Diff，Harness 使用 Mock 模型和 Mock 工具，解析模块使用固定索引样例，模型网关使用固定模型响应，测试模块先搭建自动化骨架。
3. **第一次集成**：优先合并模型网关、解析索引和 Harness 核心，跑通后端最小闭环。
4. **第二次集成**：合并前端工作台，接入真实会话、事件流、文件树和 Diff。
5. **全链路验收**：测试负责人执行从仓库导入到代码修改、测试和 Diff 交付的端到端回归，缺陷回派给对应模块负责人修复。

分支合并原则：

- 一个分支只服务一个主责模块，跨模块修改必须拆分提交或提前同步相关负责人。
- 接口变更必须先更新协议和契约测试，再修改实现；禁止直接依赖其他分支的未合并内部代码。
- 每个职责分支合并前必须通过自己的单元测试和接口契约测试。
- 集成分支只接受可运行、可回滚的变更；测试负责人可以阻止不满足质量门禁的合并。
- 紧急修复从 `main` 或当前发布分支创建，修复后同步回 `develop`/`integration`，避免分支长期漂移。

## 8. 分阶段交付建议

### 阶段一：最小可运行闭环

目标是跑通“导入目录 -> 创建会话 -> 模型调用 -> 读取文件 -> 生成补丁 -> 查看 Diff”的最短路径。

- 前端：项目入口、会话页、文件树、消息流和基础 Diff。
- Harness：单任务状态机、上下文拼装、读文件工具、补丁工具和事件流。
- 解析：文件扫描、基础语言识别、文件索引和文本搜索。
- 模型网关：单模型 Chat、流式输出和结构化工具调用。
- 测试：一条端到端冒烟链路和核心接口契约测试。

### 阶段二：仓库理解和可控执行

目标是支持真实仓库级调查和受控修改。

- 增加 AST、符号、引用和调用链检索。
- 增加计划、上下文压缩、暂停恢复、权限确认和快照回滚。
- 增加命令执行、定向测试、验证循环和错误归因。
- 建立基准仓库和第一版任务成功率指标。

### 阶段三：质量和规模化能力

目标是提高复杂任务成功率和长期稳定性。

- 增量索引、大仓库分层检索和缓存。
- 多模型路由、模型降级和成本控制。
- 多文件复杂 Diff、并发只读工具和更细粒度权限。
- 全量回归、性能压测、安全测试和发布门禁。

## 9. 首期验收标准

首期版本至少满足以下条件：

- 能导入一个完整本地源码目录，并展示文件树。
- 能建立文件索引，支持按文件名和文本搜索。
- 用户可以创建会话并提交代码任务。
- Harness 能完成至少三轮“模型决策 -> 工具执行 -> 结果观察”循环。
- 模型可以跨两个及以上文件读取源码并生成补丁。
- 所有修改都能展示前后 Diff，并能回滚到任务开始快照。
- 能执行至少一种项目测试命令，并把通过或失败结果反馈给用户。
- 用户取消、工具超时、模型异常和测试失败时，系统不会丢失任务状态。
- 每个模块有最小单元测试，系统有一条可重复的端到端验收用例。
