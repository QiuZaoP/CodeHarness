# 角色 3：代码解析与索引（首期）任务计划

## 目标

交付一个可独立运行、可测试的代码仓库索引模块：扫描目录、应用忽略规则、识别语言、建立文件索引，并支持文件名和文本检索。

## 范围与边界

- 包含：文件扫描、默认与自定义忽略规则、语言识别、内存索引、检索服务、质量报告。
- 不包含：模型调用、Harness 任务循环、前端、写文件工具、AST/符号/调用链与向量索引的具体实现。
- 对外只暴露稳定 Python API；后续 AST 与语义索引可在同一 `RepositoryIndex` 聚合模型中扩展。

## 阶段

- [x] 1. 阅读需求与确认角色 3 的交付边界。
- [x] 2. 编写详细实现计划。
- [x] 3. 在 `feature/code-indexer` 分支建立模块与测试脚手架。
- [x] 4. 以 TDD 实现扫描、忽略规则和语言识别。
- [x] 5. 以 TDD 实现文件索引、检索和质量报告。
- [x] 6. 执行完整测试与静态检查，整理交付说明。

## 验收标准

1. 递归扫描本地目录，跳过默认忽略目录及用户提供的 glob 规则。
2. 文件记录包含相对路径、语言、大小与修改时间；二进制文件不进入可搜索内容。
3. 能按路径片段、文件名和内容（大小写不敏感）返回带行号的确定性结果。
4. 不可读文件和无法解码文件被记录为扫描问题，不中止整个索引任务。
5. 单元测试覆盖核心正常路径与降级场景，测试命令退出码为 0。

## 当前状态

所有计划阶段已完成；完整测试、语法编译和 Git 空白检查均已执行。
