# 任务发现

## 2026-07-26：需求与仓库现状

- 当前仓库仅包含 `README.md`、`CONTRIBUTING.md` 与 `项目方案设计.md`，尚无生产代码、依赖管理文件或测试框架。
- 角色 3 唯一主责是“代码解析与索引”：扫描、忽略、语言识别、文件/符号/关系/语义索引及相应查询能力。
- 首期阶段的明确最低要求是文件扫描、基础语言识别、文件索引和文本搜索；AST 等深度解析属于下一阶段。
- 项目规范要求不直接在 `main` 或 `develop` 上开发，当前 Git 根目录分支为 `main`。

## 设计决策

- 采用 Python 标准库实现，避免在空仓库中引入不必要依赖。
- 使用 `src/` 布局与 `unittest`，确保任何 Python 环境均可执行测试。
- 所有路径均存为相对 POSIX 路径，保障跨平台和检索结果稳定。
