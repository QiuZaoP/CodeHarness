[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "codeharness-indexer"
version = "0.1.0"
description = "Repository scanning and text indexing for CodeHarness"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
